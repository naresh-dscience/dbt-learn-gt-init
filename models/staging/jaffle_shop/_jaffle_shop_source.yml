sources:
  - name: jaffle_shop
    database: raw  
    schema: jaffle_shop  
    tables:
      - name: orders
        freshness:
          warn_after:
            count: 1
            period: day
        loaded_at_field: _etl_loaded_at
        
      - name: customers
        freshness: null
